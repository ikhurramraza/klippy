name: Lint

on:
  pull_request:
    branches: [master]

jobs:
  build:
    name: Lint using GitHub SuperLinter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Lint Code Base
        uses: docker://github/super-linter:v3.2.0
